Z-Index {
  Relative: 0-999;
  Absolute: 1000-1999;
  Sticky: 2000-2999;
  Fixed: 3000-3999;
}

List of HTMLmodules:

  * Simple(Cannot contain modules):

    - Contained(Cannot be put outside modules)


    - Mixte(Can be put inside or outside modules)

      1. Button(.TDN-btn)
      2. List(.TDN-list)
      3. ProgressBar(.TDN-pgbar)
      4. Card(.TDN-card)
      5. Notification(.TDN-notif)
      6. Searchbar(.TDN-search)
      7. Switch(.TDN-switch)
      8. Calendar(.TDN-date)
      9. Form(.TDN-form)

    - Container(Cannot be put inside modules)

      1. Pagination(.TDN-page)

  * Complex(Can contain modules):

    - Contained(Cannot be put outside modules)

      1. Slider(.TDN-slider)
      2. Slide(.TDN-slide)
      3. Dropdown(.TDN-dp)
      4. Collapse(.TDN-clps)
      5. Popover(.TDN-ppv)
      6. Breadcrumb(.TDN-bdb)

    - Mixte(Can be put inside or outside modules)

      1. Box(.TDN-box)
      2. Carousel(.TDN-carousel)
      3. Navbar(.TDN-nav)
      4. Grid(.TDN-grid)

    - Container(Cannot be put inside modules)

      1. Header(.TDN-head)
      2. Modal(.TDN-modal)
      3. Footer(.TDN-foot)