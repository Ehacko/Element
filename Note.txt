List of HTMLmodules:

  * Simple(Cannot contain modules):

    - Contained(Cannot be put outside modules)


    - Mixte(Can be put inside or outside modules)

      1. Button(.TDNbtn)
      2. List(.TDNlist)
      3. ProgressBar(.TDNprogbar)
      4. Card(.TDNcard)
      5. Notification(.TDNnotif)
      6. Searchbar
      7. Switch
      8. Calendar

    - Container(Cannot be put inside modules)

      1. Pagination

  * Complex(Can contain modules):

    - Contained(Cannot be put outside modules)

      1. Slider
      2. Slide
      3. Menu
      4. Form
      5. Collapse
      6. Popover

    - Mixte(Can be put inside or outside modules)

      1. Carousel
      2. Navbar
      3. Grid

    - Container(Cannot be put inside modules)

      1. Header
      2. Modal
      3. Footer